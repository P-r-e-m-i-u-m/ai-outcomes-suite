# Phase 02: RAG Systems

## Goal

Build retrieval augmented generation that cites local knowledge instead of guessing.

## Build

Use `GenAiLab.retrieve()` and `GenAiLab.answerWithRag()` in `packages/genai-lab/src/index.ts`.

## Artifact

RAG patterns and example outputs live in `outputs/rag/`.

## Product Connection

- Compliance reports should cite policy sections.
- Legal letters should cite document clauses.
- SAT explanations should cite lesson material.

